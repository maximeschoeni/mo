
KarmaFieldsAlpha.field.layout.medias.row = class extends KarmaFieldsAlpha.field.layout.table.row {

  // async export(keys = []) {
  //
  // 	const object = await super.export(keys);
  //
  //   const filename = await this.parent.request("get", {}, this.resource.key, "filename");
  //
  //   object.url = `${KarmaFieldsAlpha.uploadURL}/${KarmaFieldsAlpha.Type.toString(filename)}`;
  // 	object.id = this.resource.key;
  //
  // 	return object;
  // }
  //
  // async import(object) {
  //
  //   const imageUrl = "https://commons.wikimedia.org/wiki/Commons:Photo_challenge/fr#/media/File:Squarci_di_luce_su_San_Giulio.jpg";
  //   const response = await fetch(imageUrl);
  //   const blob = await response.blob();
  //   const file = URL.createObjectURL(blob);
  //
  //
  //   const fileIds = await this.parent.request("upload", {files: [file]});
  //
  //
  //   console.log(fileIds);
  //
  //
  // }


}
